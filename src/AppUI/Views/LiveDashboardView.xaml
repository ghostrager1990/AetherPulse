<UserControl x:Class="AppUI.Views.LiveDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:AppUI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             d:DataContext="{d:DesignInstance Type=vm:LiveDashboardViewModel}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="28,20,28,28">

            <!-- Header Row -->
            <Grid Margin="0,0,0,18">
                <StackPanel>
                    <TextBlock Text="Engine Status &amp; Configuration Overview" FontSize="18" FontWeight="Bold" Foreground="#FFFFFF" />
                    <TextBlock Text="DirectX 12 SwapChain Presentation Pacing &amp; FidelityFX Ray Reconstruction active pipeline." FontSize="12" Foreground="#8B949E" Margin="0,4,0,0" />
                </StackPanel>

                <Border HorizontalAlignment="Right" VerticalAlignment="Center" Background="#161A22" CornerRadius="4" Padding="10,4" BorderBrush="#30363D" BorderThickness="1">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="7" Height="7" Stroke="#3FB950" StrokeThickness="1.5" Fill="Transparent" VerticalAlignment="Center" Margin="0,0,6,0" />
                        <TextBlock Text="{Binding ActiveGameDisplay}" FontSize="11" FontWeight="SemiBold" Foreground="#FFFFFF" />
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Metrics Cards Grid -->
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Injection Status Card -->
                <Border Grid.Column="0" Background="#161A22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="6" Padding="14" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="INJECTION STATUS" FontSize="10.5" FontWeight="Bold" Foreground="#8B949E" />
                        <TextBlock Text="{Binding InjectionStatus}" FontSize="18" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,6,0,2" />
                        <TextBlock Text="Direct VTable / D3D12 Queue" FontSize="10" Foreground="#8B949E" />
                    </StackPanel>
                </Border>

                <!-- Frame Timing Card -->
                <Border Grid.Column="1" Background="#161A22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="6" Padding="14" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="FRAME TIMING" FontSize="10.5" FontWeight="Bold" Foreground="#8B949E" />
                        <TextBlock Text="{Binding FrametimeDisplay}" FontSize="20" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,6,0,2" />
                        <TextBlock Text="D3D12 High-Res Timer" FontSize="10" Foreground="#8B949E" />
                    </StackPanel>
                </Border>

                <!-- 1% Low FPS Card -->
                <Border Grid.Column="2" Background="#161A22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="6" Padding="14" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="1% LOW FPS" FontSize="10.5" FontWeight="Bold" Foreground="#8B949E" />
                        <TextBlock Text="{Binding OnePercentLowDisplay}" FontSize="20" FontWeight="Bold" Foreground="#3FB950" Margin="0,6,0,2" />
                        <TextBlock Text="Smooth Delivery Cadence" FontSize="10" Foreground="#8B949E" />
                    </StackPanel>
                </Border>

                <!-- Pacing Jitter Card -->
                <Border Grid.Column="3" Background="#161A22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="6" Padding="14">
                    <StackPanel>
                        <TextBlock Text="PACING JITTER" FontSize="10.5" FontWeight="Bold" Foreground="#8B949E" />
                        <TextBlock Text="{Binding JitterDisplay}" FontSize="20" FontWeight="Bold" Foreground="#3FB950" Margin="0,6,0,2" />
                        <TextBlock Text="Optimal (&lt; 1.0% Target)" FontSize="10" Foreground="#8B949E" />
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Active D3D12 Pipeline Containers -->
            <Border Background="#161A22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="6" Margin="0,0,0,20" Padding="18">
                <StackPanel>
                    <TextBlock Text="Active Direct3D 12 Native Pipeline" FontSize="14" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,14" />
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Pacing Hook Box -->
                        <Border Grid.Column="0" Background="#0D1117" BorderBrush="#30363D" BorderThickness="1" CornerRadius="6" Padding="14" Margin="0,0,8,0">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="⚡" Foreground="#E01E26" FontSize="13" Margin="0,0,6,0" VerticalAlignment="Center" />
                                    <TextBlock Text="Presentation Frame Pacer" FontWeight="Bold" FontSize="12.5" Foreground="#E01E26" VerticalAlignment="Center" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <Ellipse Width="8" Height="8" Stroke="{Binding PacingIndicatorColor}" StrokeThickness="1.5" Fill="Transparent" VerticalAlignment="Center" Margin="0,0,6,0" />
                                    <TextBlock Text="{Binding PacingPipelineStatus}" FontSize="11" FontWeight="SemiBold" Foreground="#FFFFFF" />
                                </StackPanel>
                                <TextBlock Text="• High-resolution waitable timer (CREATE_WAITABLE_TIMER_HIGH_RESOLUTION)" FontSize="10.5" Foreground="#8B949E" Margin="0,0,0,2" />
                                <TextBlock Text="• Half-interval cadence alignment for fluid frame generation" FontSize="10.5" Foreground="#8B949E" Margin="0,0,0,2" />
                                <TextBlock Text="• AMD Radeon Anti-Lag 2 SDK synchronous CPU/GPU pacing" FontSize="10.5" Foreground="#8B949E" />
                            </StackPanel>
                        </Border>

                        <!-- Ray Reconstruction Box -->
                        <Border Grid.Column="1" Background="#0D1117" BorderBrush="#30363D" BorderThickness="1" CornerRadius="6" Padding="14" Margin="8,0,0,0">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="✨" Foreground="#3FB950" FontSize="13" Margin="0,0,6,0" VerticalAlignment="Center" />
                                    <TextBlock Text="FidelityFX Ray Reconstruction" FontWeight="Bold" FontSize="12.5" Foreground="#3FB950" VerticalAlignment="Center" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <Ellipse Width="8" Height="8" Stroke="{Binding RayRegenIndicatorColor}" StrokeThickness="1.5" Fill="Transparent" VerticalAlignment="Center" Margin="0,0,6,0" />
                                    <TextBlock Text="{Binding RayRegenPipelineStatus}" FontSize="11" FontWeight="SemiBold" Foreground="#FFFFFF" />
                                </StackPanel>
                                <TextBlock Text="• Intercepts D3D12 Ray Tracing radiance passes" FontSize="10.5" Foreground="#8B949E" Margin="0,0,0,2" />
                                <TextBlock Text="• Real-time bilateral spatial/temporal filtering" FontSize="10.5" Foreground="#8B949E" Margin="0,0,0,2" />
                                <TextBlock Text="• Direct compute dispatch via FidelityFX Denoiser &amp; NRC architecture" FontSize="10.5" Foreground="#8B949E" />
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- System Environment Specifications -->
            <Border Background="#161A22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="6" Padding="18">
                <StackPanel>
                    <TextBlock Text="System Environment Specifications" FontSize="14" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,14" />
                    
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <!-- Col 0 -->
                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,0,12">
                            <TextBlock Text="Graphics Card" FontSize="10.5" Foreground="#8B949E" />
                            <TextBlock Text="{Binding GpuName}" FontSize="12" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,2,0,0" />
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="0">
                            <TextBlock Text="Dedicated VRAM" FontSize="10.5" Foreground="#8B949E" />
                            <TextBlock Text="{Binding DedicatedVram}" FontSize="12" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,2,0,0" />
                        </StackPanel>

                        <!-- Col 1 -->
                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,12">
                            <TextBlock Text="Display Adapter Driver" FontSize="10.5" Foreground="#8B949E" />
                            <TextBlock Text="{Binding DriverVersion}" FontSize="12" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,2,0,0" />
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="1">
                            <TextBlock Text="Primary Display Mode" FontSize="10.5" Foreground="#8B949E" />
                            <TextBlock Text="{Binding PrimaryDisplayMode}" FontSize="12" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,2,0,0" />
                        </StackPanel>

                        <!-- Col 2 -->
                        <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,12">
                            <TextBlock Text="Processor (CPU)" FontSize="10.5" Foreground="#8B949E" />
                            <TextBlock Text="{Binding CpuName}" FontSize="12" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,2,0,0" />
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="2">
                            <TextBlock Text="Core Health" FontSize="10.5" Foreground="#8B949E" />
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                <Ellipse Width="8" Height="8" Stroke="#3FB950" StrokeThickness="1.5" Fill="Transparent" VerticalAlignment="Center" Margin="0,0,6,0" />
                                <TextBlock Text="{Binding CoreHealthStatus}" FontSize="12" FontWeight="Bold" Foreground="#3FB950" VerticalAlignment="Center" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
