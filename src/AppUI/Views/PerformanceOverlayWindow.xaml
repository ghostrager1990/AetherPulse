<Window x:Class="AppUI.Views.PerformanceOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AetherPulse HUD Overlay"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        FontFamily="Bahnschrift"
        Width="340" Height="115"
        Left="24" Top="24">

    <Border Background="#F20D1117" 
            BorderBrush="#30363D" 
            BorderThickness="1" 
            CornerRadius="8" 
            Padding="14,10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Row 0: Title & Status Badge -->
            <Grid Grid.Row="0" Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Image Source="pack://application:,,,/Assets/app_icon.png" Width="14" Height="14" Margin="0,0,6,0" VerticalAlignment="Center"/>
                    <TextBlock Text="AETHERPULSE HUD" FontWeight="Bold" FontSize="11" Foreground="#E6EDF3" VerticalAlignment="Center"/>
                </StackPanel>
                <Border Grid.Column="1" x:Name="BadgeStatus" Background="#1C2F24" BorderBrush="#238636" BorderThickness="1" CornerRadius="4" Padding="6,2" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtStatus" Text="STANDBY" FontSize="8.5" FontWeight="Bold" Foreground="#3FB950"/>
                </Border>
            </Grid>

            <!-- Row 1: Pacing Engine Status Chip -->
            <Grid Grid.Row="1" Margin="0,0,0,8">
                <Border x:Name="ChipPacing" Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="3" Padding="6,2" Opacity="0.5">
                    <TextBlock x:Name="TxtPacingMode" Text="CADENCE PACING: INACTIVE" FontSize="8.5" FontWeight="SemiBold" Foreground="#8B949E" TextAlignment="Center"/>
                </Border>
            </Grid>

            <!-- Row 2: Main Performance Metrics & Cadence Ratio -->
            <Grid Grid.Row="2" Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,12,0">
                    <TextBlock Text="FPS" FontSize="10" FontWeight="SemiBold" Foreground="#8B949E" VerticalAlignment="Bottom" Margin="0,0,3,3"/>
                    <TextBlock x:Name="TxtFps" Text="0.0" FontSize="21" FontWeight="Bold" Foreground="#FFFFFF" VerticalAlignment="Bottom"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,12,0">
                    <TextBlock Text="FT" FontSize="10" FontWeight="SemiBold" Foreground="#8B949E" VerticalAlignment="Bottom" Margin="0,0,3,3"/>
                    <TextBlock x:Name="TxtFrametime" Text="0.00 ms" FontSize="15" FontWeight="Bold" Foreground="#00FFCC" VerticalAlignment="Bottom"/>
                </StackPanel>

                <Border Grid.Column="2" Background="#162235" BorderBrush="#1F6FEB" BorderThickness="1" CornerRadius="4" Padding="6,2" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="CADENCE: " FontSize="9" FontWeight="SemiBold" Foreground="#8B949E" VerticalAlignment="Center"/>
                        <TextBlock x:Name="TxtCadence" Text="50:50" FontSize="10.5" FontWeight="Bold" Foreground="#58A6FF" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Row 3: Telemetry Sub-metrics -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="0,0,10,0">
                    <TextBlock Text="Jitter: " FontSize="9.5" Foreground="#8B949E"/>
                    <TextBlock x:Name="TxtJitter" Text="0 µs" FontSize="9.5" FontWeight="SemiBold" Foreground="#C9D1D9"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,10,0">
                    <TextBlock Text="1% Low: " FontSize="9.5" Foreground="#8B949E"/>
                    <TextBlock x:Name="TxtLowFps" Text="0.0" FontSize="9.5" FontWeight="SemiBold" Foreground="#FFAA00"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock Text="Delta: " FontSize="9.5" Foreground="#8B949E"/>
                    <TextBlock x:Name="TxtDelta" Text="0.0ms" FontSize="9.5" FontWeight="SemiBold" Foreground="#58A6FF"/>
                </StackPanel>

                <TextBlock Grid.Column="3" Text="Ctrl+Shift+[" FontSize="10" FontWeight="SemiBold" Foreground="#8B949E" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Grid>
        </Grid>
    </Border>
</Window>



