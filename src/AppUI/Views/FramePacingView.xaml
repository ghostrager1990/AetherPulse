<UserControl x:Class="AppUI.Views.FramePacingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:vm="clr-namespace:AppUI.ViewModels"
             FontFamily="Bahnschrift"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=vm:FramePacingViewModel}"
             d:DesignHeight="900" d:DesignWidth="850">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <Style TargetType="Button" x:Key="PresetBtnStyle">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#161B22"/>
            <Setter Property="Foreground" Value="#8B949E"/>
            <Setter Property="BorderBrush" Value="#30363D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#58A6FF"/>
                    <Setter Property="Foreground" Value="#58A6FF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="28,20,28,28">

            <!-- Header -->
            <Grid Margin="0,0,0,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,20,0" VerticalAlignment="Center">
                    <TextBlock FontSize="20" FontWeight="Bold" Foreground="#FFFFFF" Text="Frame Pacing &amp; Smooth Delivery Tuning"/>
                    <TextBlock FontSize="13" Foreground="#8B949E" LineHeight="17" Margin="0,4,0,0" Text="Configure microsecond swapchain frame timing, target frame caps, and sub-frame cadence synchronization." TextWrapping="Wrap"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Background="#161B22" BorderBrush="#3D1418" BorderThickness="1" Command="{Binding ResetDefaultsCommand}" Content="â†º Reset to Defaults" Cursor="Hand" FontSize="12" FontWeight="SemiBold" Foreground="#FF6E6E" Height="32" Padding="14,0" Margin="0,0,10,0"/>
                    <ComboBox ItemsSource="{Binding DataContext.AvailableTuningProfiles, RelativeSource={RelativeSource AncestorType=Window}}"
                              SelectedItem="{Binding DataContext.ActiveTuningProfile, RelativeSource={RelativeSource AncestorType=Window}}"
                              SelectedValue="{Binding DataContext.ActiveTuningProfile.GameName, RelativeSource={RelativeSource AncestorType=Window}}"
                              SelectedValuePath="GameName"
                              DisplayMemberPath="GameName"
                              Style="{StaticResource DarkComboBox}"
                              Width="220" Height="32" VerticalAlignment="Center" />
                </StackPanel>
            </Grid>

            

            <!-- Card 2: Frame Generation Cadence Multiplier -->
            <Border Background="#161A22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="6" Margin="0,0,0,16" Padding="18">
                <StackPanel>
                    <TextBlock FontSize="15" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,14" Text="Frame Generation Cadence &amp; Sub-Frame Multiplier"/>

                    <!-- Multiplier Selection Buttons -->
                    <TextBlock FontSize="12" Foreground="#8B949E" Margin="0,0,0,8" Text="Optical-Flow Cadence Alignment Mode:"/>
                    <StackPanel Margin="0,0,0,16" Orientation="Horizontal">
                        <Button Command="{Binding SetMultiplierCommand}" CommandParameter="1X" Content="1X (Native)" Style="{StaticResource PresetBtnStyle}"/>
                        <Button Command="{Binding SetMultiplierCommand}" CommandParameter="2X" Content="2X FG (50/50)" Style="{StaticResource PresetBtnStyle}"/>
                        <Button Command="{Binding SetMultiplierCommand}" CommandParameter="3X" Content="3X FG (33/66)" Style="{StaticResource PresetBtnStyle}"/>
                        <Button Command="{Binding SetMultiplierCommand}" CommandParameter="4X" Content="4X FG (Quad)" Style="{StaticResource PresetBtnStyle}"/>
                    </StackPanel>

                    <Border Background="#0D1117" BorderBrush="#1F6FEB" BorderThickness="1" CornerRadius="6" Padding="12,10">
                        <TextBlock FontSize="12" Foreground="#58A6FF" Text="Cadence Mode: Forward sub-frame timeline synchronization active. Aligns real and interpolated frame intervals without artificial frame capping." TextWrapping="Wrap"/>
                    </Border>
                </StackPanel>
            </Border>

            <!-- Card 3: Cadence Diagnostics Matrix -->
            <Border Background="#11141B" BorderBrush="#1F6FEB" BorderThickness="1" CornerRadius="6" Padding="16">
                <StackPanel>
                    <Grid Margin="0,0,0,12">
                        <TextBlock FontSize="14" FontWeight="Bold" Foreground="#58A6FF" Text="ðŸ” Presentation Cadence Diagnostics" VerticalAlignment="Center"/>
                        <CheckBox HorizontalAlignment="Right" IsChecked="{Binding IsDebugViewExpanded, Mode=TwoWay}" Style="{StaticResource ModernToggle}" VerticalAlignment="Center"/>
                    </Grid>

                    <Grid Visibility="{Binding IsDebugViewExpanded, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="4" Grid.Column="0" Margin="0,0,8,0" Padding="10">
                            <StackPanel>
                                <TextBlock FontSize="10" FontWeight="Bold" Foreground="#8B949E" Text="CADENCE DISTRIBUTION"/>
                                <TextBlock FontSize="18" FontWeight="Bold" Foreground="#00FFCC" Margin="0,4,0,2" Text="50 : 50"/>
                                <TextBlock FontSize="10" Foreground="#7EE787" Text="Real vs Interpolated Ratio"/>
                            </StackPanel>
                        </Border>

                        <Border Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="4" Grid.Column="1" Margin="0,0,8,0" Padding="10">
                            <StackPanel>
                                <TextBlock FontSize="10" FontWeight="Bold" Foreground="#8B949E" Text="SUB-FRAME JITTER"/>
                                <TextBlock FontSize="18" FontWeight="Bold" Foreground="#D2A8FF" Margin="0,4,0,2" Text="&lt; 0.5 ms"/>
                                <TextBlock FontSize="10" Foreground="#8B949E" Text="Microsecond presentation variance"/>
                            </StackPanel>
                        </Border>

                        <Border Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="4" Grid.Column="2" Padding="10">
                            <StackPanel>
                                <TextBlock FontSize="10" FontWeight="Bold" Foreground="#8B949E" Text="HOOK PIPELINE STATUS"/>
                                <TextBlock FontSize="13" FontWeight="Bold" Foreground="#58A6FF" Margin="0,6,0,2" Text="{Binding PacerHookStatus}"/>
                                <TextBlock FontSize="9.5" Foreground="#7EE787" Text="Direct DXGI VMT Interceptor"/>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>




